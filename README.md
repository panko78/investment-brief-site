# 每日投资简报站

这是一个纯静态、数据驱动的网站。

## 文件
- `index.html`：网页
- `brief.json`：每天的简报数据

## 发布
可部署到 GitHub Pages、Cloudflare Pages、Netlify 等任意静态托管。

## 每日更新
每天只需要覆盖 `brief.json`，网页结构无需变化。
建议由自动任务在每天 07:30 生成新的 `brief.json` 后提交到仓库。
